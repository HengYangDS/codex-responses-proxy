# Repository-truth ledger for falsifiable Codex DMX Proxy evolution claims.
# Evidence and OpenSpec records own results and decisions; this ledger states
# the challenge that must continue to be tested.

[[hypothesis]]
id = "provider-portable-responses-remain-fail-closed"
campaign = "dmxapi-reliability-closeout"
state = "active"
owner = "codex-dmx-proxy-maintainers"
claim = "Provider-portable Responses projection can preserve structurally valid empty tool outputs without admitting empty dialogue, malformed pairs, provider-bound identifiers, or ciphertext."
challenge = "A replay shape accepted by the projection may still leak provider state, weaken pairing rules, or reintroduce empty-content and bounded-503 failures in the installed runtime."
transition = "source repair -> signed dual-Forge release -> installed runtime observation -> corrective closeout"
proof_refs = [
  "ethos prove --execute --json",
  "scripts/run-python-quality.sh",
  "tests/test_provider_portable_responses.py",
]
review_refs = [
  "evidence/chronicle/dmxapi-reliability-closeout-20260731/2026-07-31.md",
  "docs/architecture/authority-and-runtime-boundary.md",
]
decision_refs = [
  "openspec/changes/archive/2026-07-31-dmxapi-reliability-closeout/design.md",
  "openspec/specs/provider-portable-responses/spec.md",
]
retirement_conditions = [
  "the projection is replaced by a narrower owner with equivalent fail-closed tests and migration evidence",
  "or live evidence falsifies the claim and the installed release is rolled back or superseded",
]
