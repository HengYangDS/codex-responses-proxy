schema_version = 1
id = "change:github-container-safe-directory"
intent = "Make the GitHub Linux release container read the exact checked-out commit without globally trusting unrelated repositories, then release the immutable forward fix as v2.0.27."
subjects = [
  "repository:self",
]
scope = [
  "openspec/changes/github-container-safe-directory/**",
  ".github/workflows/verify.yml",
  "tests/forge/test_workflow_contracts.py",
  "VERSION",
  "CHANGELOG.md",
  "README.md",
]
invariants = []
acceptance = []
risks = []
authority_refs = []
permissions = [
  "repository.read",
  "work-lane.write",
]
hypotheses = []
dependencies = []
