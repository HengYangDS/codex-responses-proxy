schema: spec-driven
created: 2026-09-01
goal: Replace the upstream urllib transport with one HTTPX implementation while
  preserving byte, SSE, timeout, recovery, proxy-isolation, frozen-release, and
  native-platform contracts.
skip_specs: true
