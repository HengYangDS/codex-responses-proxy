# Design

Use workflow-scoped `GIT_CONFIG_*` variables for stateless Git initialization.
Give only the concurrent macOS matrix a `cache-suffix` derived from its existing
Python dimension; all other jobs retain their current cache identity. The
repository contract remains the single policy owner.
