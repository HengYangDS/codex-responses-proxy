schema_version = 1
id = "change:forge-projection-sequence"
intent = "Make explicit Forge continuity map only the canonical suffix after its exact base so duplicate historical fingerprints outside that suffix cannot block a verified forward-only checkpoint."
subjects = ["repository:codex-responses-proxy"]
scope = [
  "docs/operations/forge-operations.md",
  "openspec/changes/forge-projection-sequence/**",
  "openspec/changes/archive/*-forge-projection-sequence/**",
  "openspec/specs/ci-diagnostics/spec.md",
  "tests/forge/**",
  "tools/forge/**",
]
invariants = [
  "both_forges_remain_independent",
  "published_refs_and_tags_are_never_rewritten",
  "explicit_continuity_requires_exact_base_anchor_and_tip",
  "ordinary_projection_remains_strict",
]
acceptance = [
  "duplicates_before_the_explicit_base_do_not_block",
  "duplicates_after_the_explicit_base_fail_closed",
  "provider_tip_drift_fails_before_mutation",
  "complete_repository_proof_passes",
]
risks = [
  "mapping_an_ambiguous_successor",
  "weakening_ordinary_projection",
]
authority_refs = [
  "AGENTS.md",
  "docs/operations/forge-operations.md",
  "openspec/specs/ci-diagnostics/spec.md",
]
hypotheses = [
  "explicit_base_sequence_position_disambiguates_only_the_successor_suffix",
]
dependencies = []
