schema_version = 1
id = "change:release-signing-file-identity"
intent = "Preserve secure provider-owned signing-key file identity when its content is already complete, and normalize only incomplete POSIX file-variable input."
subjects = [
  "repository:self",
]
scope = [
  "openspec/changes/release-signing-file-identity/**",
  "tools/release/signing.py",
  "tests/release/test_signing.py",
  "CHANGELOG.md",
  "VERSION",
]
invariants = []
acceptance = []
risks = []
authority_refs = []
permissions = [
  "repository.read",
  "work-lane.write",
]
hypotheses = []
dependencies = []
