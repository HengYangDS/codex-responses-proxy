schema_version = 1
id = "change:windows-bundle-path-forward-fix"
intent = "Make native bundle containment compare canonical filesystem identity on Windows and publish the failed release forward without weakening escape rejection."
subjects = ["repository:self"]
scope = [
  "CHANGELOG.md",
  "README.md",
  "VERSION",
  "openspec/changes/windows-bundle-path-forward-fix/**",
  "openspec/changes/archive/*-windows-bundle-path-forward-fix/**",
  "tools/release/assets.py",
  "tests/release/test_assets.py",
]
invariants = [
  "bundle_members_outside_the_resolved_bundle_remain_rejected",
  "symlink_cycles_and_non_regular_members_remain_rejected",
  "windows_case_insensitive_path_identity_is_respected",
  "failed_v2_0_17_tags_runs_and_release_absence_remain_immutable",
  "gitlab_and_github_remain_independent_publication_planes",
]
acceptance = [
  "the_windows_case_variant_regression_fails_before_the_fix_and_passes_after_it",
  "documentation_and_release_identity_are_exactly_aligned",
  "all_python_platform_quality_release_and_hosted_gates_pass",
]
risks = ["filesystem_case_semantics_differ_across_supported_platforms"]
authority_refs = ["tools/release/assets.py", "tests/release/test_assets.py", "README.md"]
permissions = ["repository.read", "work-lane.write", "local-proof.execute", "git.ref.compare-and-swap", "terminal-publication.execute"]
hypotheses = ["canonical_normcase_commonpath_comparison_removes_the_false_windows_escape_without_weakening_containment"]
dependencies = []
