schema_version = 1
id = "change:commitment-schema-migration"
intent = "Migrate the repository Commitment to the current ETHOS schema without changing product behavior."
subjects = ["repository:codex-responses-proxy"]
scope = [".ethos/commitment.toml", "openspec/specs/README.md"]
invariants = [
  "product_behavior_is_unchanged",
  "repository_authority_remains_portable",
]
acceptance = [
  "retired_permissions_are_removed",
  "openspec_entrypoint_is_present",
  "governed_plan_is_unblocked",
]
authority_refs = ["user_instruction", "AGENTS.md", ".ethos/profile.toml"]
permissions = ["repository.read", "work-lane.write", "git.ref.compare-and-swap"]
dependencies = []
