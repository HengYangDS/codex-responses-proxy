schema_version = 1
id = "change:publish-python-identity"
intent = "Bind GitLab release execution to the Python environment synchronized by uv and cache its managed runtime downloads."
subjects = ["repository:self"]
scope = [
  "openspec/changes/publish-python-identity/**",
  ".config/**",
  ".gitlab-ci.yml",
  ".github/workflows/*.yml",
  "tests/**",
]
invariants = [
  "ci_semantics_have_one_declarative_owner",
  "release_execution_uses_the_exact_synchronized_python_identity",
  "forge_projections_remain_generated_outputs",
]
acceptance = [
  "gitlab_release_uses_explicit_python_without_download_or_sync_drift",
  "uv_managed_python_downloads_are_cached_by_target_platform",
  "focused_ci_contracts_and_complete_quality_gates_pass",
]
risks = ["cache_identity_can_drift_if_target_platform_dimensions_are_incomplete"]
authority_refs = ["AGENTS.md", ".gitlab-ci.yml", "pyproject.toml", "uv.lock"]
hypotheses = []
dependencies = []
