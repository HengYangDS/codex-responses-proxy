schema_version = 1
id = "change:latest-stable-python-toolchain"
intent = "Converge the locked Python quality toolchain to current stable releases without changing product behavior or adding a second dependency authority."
subjects = [
  "repository:self",
]
scope = [
  "pyproject.toml",
  "uv.lock",
  "openspec/changes/latest-stable-python-toolchain/**",
]
invariants = [
  "pyproject.toml remains the direct dependency declaration and uv.lock remains the only resolved Python dependency authority.",
  "The update does not change runtime behavior, supported Python versions, product boundaries, or release semantics.",
  "No compatibility layer, wrapper, alternate environment manager, or host-specific path is introduced.",
]
acceptance = [
  "Every declared Python dependency resolves to its current stable version allowed by the repository contract.",
  "uv lock verification and all repository quality, Python 3.12, 3.13, 3.14, and release gates pass from the exact final HEAD.",
  "The dependency diff contains only intentional current-stable toolchain convergence and its necessary transitive resolution.",
]
risks = [
  "A quality-tool update can expose new diagnostics or change lock resolution and must fail closed under the existing full gate set.",
]
authority_refs = [
  "pyproject.toml",
  "uv.lock",
  "CONTRIBUTING.md",
]
permissions = [
  "repository.read",
  "work-lane.write",
]
hypotheses = []
dependencies = []
