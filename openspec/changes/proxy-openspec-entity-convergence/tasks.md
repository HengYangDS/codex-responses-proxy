## 1. Remove parallel carriers

- [x] 1.1 Delete archived Change README summaries after verifying every affected Change retains `proposal.md`.
- [x] 1.2 Delete archived `scope.toml` inventories after verifying no current source, test, tool, configuration, or documentation consumes them.
- [x] 1.3 Delete current and archived `capability.toml` descriptors after verifying canonical specifications retain the normative behavior.
- [x] 1.4 Delete the empty `openspec/specs/README.md` index and verify OpenSpec still lists every canonical specification.

## 2. Prove the single authority

- [x] 2.1 Validate all OpenSpec artifacts with `openspec validate --all --strict`.
- [x] 2.2 Run focused repository organization and documentation quality tests.
- [x] 2.3 Search the tracked tree for retired carrier classes and verify only the explicitly deferred ETHOS Commitment carrier remains.
- [x] 2.4 Verify official archive readiness with complete tasks and strict OpenSpec validation.
