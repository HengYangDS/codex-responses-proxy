schema: spec-driven
created: 2026-08-04
goal: Bound one provider route-slot hold by a total upstream stream deadline and
  name the binding route width in a retryable queue-timeout denial, without
  widening route single-flight.
