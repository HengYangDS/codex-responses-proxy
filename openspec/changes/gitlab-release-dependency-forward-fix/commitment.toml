id = "change:gitlab-release-dependency-forward-fix"
intent = "Ensure GitLab release publication installs the locked project runtime dependencies required by repository release tools."
subjects = ["repository:self"]
scope = [
  ".gitlab-ci.yml",
  "tests/forge/test_workflow_contracts.py",
  "tests/quality/test_verification.py",
  "openspec/changes/gitlab-release-dependency-forward-fix/**",
  "openspec/changes/archive/*-gitlab-release-dependency-forward-fix/**",
]
invariants = [
  "release_publication_uses_the_repository_locked_python_environment",
  "gitlab_and_github_publication_remain_independent",
  "product_runtime_behavior_remains_unchanged",
]
acceptance = [
  "the_gitlab_publish_job_installs_project_and_quality_dependencies",
  "focused_workflow_contract_tests_pass",
  "complete_repository_gates_pass",
  "gitlab_release_publication_succeeds",
]
risks = ["release_environment_dependency_drift"]
authority_refs = ["AGENTS.md", ".gitlab-ci.yml", "pyproject.toml"]
hypotheses = ["the_failed_job_lacked_cyclopts_because_only_the_quality_group_was_installed_without_the_project"]
dependencies = []
