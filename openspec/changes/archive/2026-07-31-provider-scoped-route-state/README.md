# provider-scoped-route-state

Align proxy-owned reversible route state and control evidence with AIGW's
provider-scoped loopback endpoints before v1.0.44 publication. The existing
`adopt-aigw` command is the sole schema-v2 to schema-v3 migration entry; the
bounded direct-Codex `/v1` route remains a separate legacy compatibility path.
