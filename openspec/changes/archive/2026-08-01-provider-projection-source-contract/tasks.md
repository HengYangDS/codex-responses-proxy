## 1. Specify and reproduce

- [x] 1.1 Separate accepted-source and provider-target branch authority.
- [x] 1.2 Bind the repair to `ci-diagnostics` and the existing Claim.
- [x] 1.3 Add failing fixtures for canonical `dev` projection and traceback-free
  runner failure.

## 2. Implement and prove

- [x] 2.1 Make GitHub source selection explicit and target only remote `main`.
- [x] 2.2 Preserve child diagnostics and exit status without Python traceback.
- [x] 2.3 Pass focused and full local gates, the Python 3.12-3.14 matrix,
  statement and branch coverage above 95%, strict OpenSpec, and a zero-match
  prohibited-diagnostic scan of retained local logs.
- [x] 2.4 Align provider signing fingerprints, trust anchors, and OpenSSH-agent inputs; defer signed-commit and exact-HEAD executed proof to source closeout.
- [x] 2.5 Rename every current product identity carrier to Codex Responses Proxy,
  retain provider names only for real profiles or wire semantics, and remove the
  old executable namespace without a compatibility shim.
- [x] 2.6 Transfer Forge-project rename, formal publication, installed-runtime migration, and service projection proof to the post-archive operational sequence without claiming those external transitions in this source carrier.
- [x] 2.7 Force `store=false` on normal and bounded-recovery requests, remove
  provider continuation and item identities, and prove classified DMX retries
  reuse byte-identical stateless bytes.
- [x] 2.8 Build deterministic release assets from immutable Git blobs, publish
  them through both Forge-native release planes, and require complete cross-Forge
  digest equality in publication proof.

## 3. Source closeout

- [x] 3.1 Bind the completed source change to the active Claim and strict archive preflight; transfer signed commit, executed proof, official archive, and archive-commit proof to the owner-bound source-closeout sequence.

## Post-archive operational sequence

These are external lifecycle transitions, not source-change tasks. Their truth
must be established from fresh accepted-root, Forge, runtime, record, and
repository-family evidence:

1. Re-land candidate and accepted roots without creating another Work Lane.
2. Project both provider histories; require exact-tip hosted CI and clean job
   logs while preserving the existing `v1.0.45` tags and Releases.
3. Refresh bounded publication, asset, installation, listener, serving-digest,
   route-authority, and reliability evidence.
4. Reclose the Claim and Chronicle, create and verify the immutable corrective
   record, remove only owned transient residue, and retire this sole own lane.
