# Design

The canonical requirement already exists under
`openspec/specs/ci-diagnostics/spec.md`. This Change is intentionally
specification-neutral: it closes the generation-scope authority gap without
adding a second semantic surface or replaying the prior delta.

The new Commitment names the canonical path. Archiving this Change advances the
generation boundary so subsequent status and proof evaluate one current,
fully-covered state.
