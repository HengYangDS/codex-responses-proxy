schema_version = 1
id = "change:publication-land-authority"
intent = "Complete the publication-topology lifecycle with explicit candidate landing authority."
subjects = [
    "repository:self",
]
scope = [
    "openspec/changes/publication-land-authority/**",
    "openspec/specs/product-interface/spec.md",
    "openspec/changes/archive/*-publication-land-authority/**",
    ".ethos/release.toml",
    "openspec/changes/archive/2026-08-07-declare-publication-topology/**",
    ".gitleaks.toml",
]
invariants = [
    "candidate_landing_uses_only_the_governed_exact_cas_operation",
    "work_and_candidate_refs_remain_local_only",
    "product_source_runtime_and_forge_topology_remain_unchanged",
]
acceptance = [
    "strict_openspec_validation_passes",
    "exact_head_repository_proof_passes",
    "archive_and_candidate_landing_complete_through_ethos",
]
risks = ["authority_scope_widening"]
authority_refs = ["user_instruction", "AGENTS.md", "rules/mutation.md"]
permissions = [
    "repository.read",
    "work-lane.write",
    "git.ref.compare-and-swap",
]
hypotheses = []
dependencies = []
