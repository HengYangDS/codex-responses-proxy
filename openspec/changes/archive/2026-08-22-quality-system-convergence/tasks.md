## 1. Quality Contract

- [x] 1.1 Inventory every tracked carrier, current gate, tool-native policy, and uncovered concern; verify the responsibility map has no uncovered or multiply owned carrier.
- [x] 1.2 Implement the canonical responsibility-driven quality map with risk, measurement, remediation, false-positive cost, and review condition fields; verify malformed or incomplete policy fails focused contract tests.

## 2. Python Quality

- [x] 2.1 Normalize imports and enable the justified Ruff correctness, modernization, naming, exception, logging, security, pytest, complexity, and performance families for each code role; verify Ruff check and format pass without inline suppression or blanket exclusions.
- [x] 2.2 Enforce complete Google-style public API documentation and signature consistency for product and tool code while keeping test names authoritative; verify documentation checks pass and deliberate public API gaps fail focused tests.
- [x] 2.3 Enable all applicable ty diagnostics across Python 3.12 semantics and all supported platforms; repair every diagnostic at its owning boundary and verify warnings-as-errors passes without `Any`-based or suppression-based escapes.
- [x] 2.4 Enforce dependency direction, dependency declarations, dead code, and justified complexity boundaries; verify the native tools and repository architecture tests pass with no baseline file.

## 3. Repository Surfaces

- [x] 3.1 Add mature formatting and linting for Markdown, YAML, TOML, CUE, links, workflows, secrets, and executable text; verify every tracked non-binary carrier is covered exactly once.
- [x] 3.2 Validate commit grammar, Decision Records, OpenSpec, release identity, and generated CI projections through their canonical owners; verify representative invalid inputs fail focused contract tests.

## 4. Projection and Proof

- [x] 4.1 Make quick, quality, governance, full, and release sessions compose the complete graph without duplicate work; verify hooks, GitHub, and GitLab invoke only those repository-owned sessions.
- [x] 4.2 Run focused quality tests, Ruff format/check, ty all-rules, dependency/dead-code/security/prose/configuration checks, Python 3.12-3.14 full tests, release construction, OpenSpec strict validation, and diff checks; record exact green evidence.
- [x] 4.3 Archive the completed change, rerun the terminal graph from a clean signed commit, and verify no obsolete quality carrier, waiver, baseline, or parallel command remains.
