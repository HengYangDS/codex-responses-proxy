schema_version = 1
id = "change:forge-independent-release-metadata"
intent = "Make each Forge release pipeline validate only its own provider state while preserving shared release policy and post-publication parity auditing."
subjects = [
    "repository:self",
]
scope = [
    "openspec/changes/forge-independent-release-metadata/**",
    "VERSION",
    "CHANGELOG.md",
    ".gitlab-ci.yml",
    "tests/release/test_metadata.py",
    "tools/release/metadata.py",
]
invariants = []
acceptance = []
risks = []
authority_refs = []
permissions = [
    "repository.read",
    "work-lane.write",
]
hypotheses = []
dependencies = []
