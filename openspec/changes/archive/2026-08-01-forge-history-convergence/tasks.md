## 1. Contract

- [x] 1.1 Align release policy with independent append-only Forge identities.
- [x] 1.2 Split the oversized CI requirement into MECE invariants.
- [x] 1.3 Bind this continuation to the existing active CI Claim.

## 2. Convergence

- [x] 2.1 Verify the exact GitLab tip and its unique identity-neutral base.
- [x] 2.2 Replay only unpublished accepted descendants onto that tip.
- [x] 2.3 Sign every successor commit with the GitLab identity and preserve the
  terminal candidate tree.

## 3. Proof

- [x] 3.1 Pass diagnostic-free strict OpenSpec validation and focused metadata
  contracts.
- [x] 3.2 Pass the full Python 3.12-3.14, statement, branch, documentation, and
  repository quality gates.
- [x] 3.3 Transfer exact-HEAD ETHOS proof, local protected-ref convergence,
  landing, and remote publication to the owner-bound post-archive sequence
  without claiming those external transitions here.

## Post-archive operational sequence

These are lifecycle transitions whose truth must be established from fresh
Git, ETHOS, Forge, release, and runtime evidence after this source change is
archived:

1. Create the signed archive commit and advance its Work Lane lease by the
   exact prepared and committed ref transaction.
2. Execute exact-HEAD ETHOS proof, converge the local candidate and accepted
   refs without force-updating either Forge, and complete governed landing.
3. Fast-forward GitLab, append the GitHub identity projection, require exact-tip
   hosted CI on both Forges, and publish the signed `v2.0.0` releases.
4. Download both release asset sets and prove byte-for-byte and SHA-256
   equality before installation or runtime acceptance.
