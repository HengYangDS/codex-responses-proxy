# Design

Use the public ETHOS adoption migration as the sole producer. The migration
removes retired fields and creates the missing OpenSpec entrypoint without
changing existing nonempty bindings. A receipt-bound Commitment rebind moves
the Work Lane from its legacy carrier generation to this exact tree.
