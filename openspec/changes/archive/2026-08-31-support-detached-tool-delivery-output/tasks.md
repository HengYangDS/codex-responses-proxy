## 1. Protocol Contract

- [x] 1.1 Add an exact regression for the observed standalone namespaced
  function output and verify the current projector rejects it.
- [x] 1.2 Add malformed hybrid and incomplete standalone-output regressions and
  verify they remain fail-closed.

## 2. Portable Projection

- [x] 2.1 Project the bounded standalone delivery into one deterministic
  provider-neutral message and verify paired call/output behavior is unchanged.

## 3. Verification

- [x] 3.1 Validate the OpenSpec Change and pass the focused protocol and relay
  suites.
- [x] 3.2 Pass the repository quality and release gates before lifecycle
  promotion.
