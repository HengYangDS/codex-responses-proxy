# Forward-fix the GitHub release-key failure

`v2.0.16` remains immutable failure evidence. This Change advances the proven
key-materialization repair to the next patch without modifying runtime behavior.
