schema_version = 1
id = "change:release-2-0-44"
intent = "Advance the proxy release identity to 2.0.44 after the validated runtime supply-chain refresh and preserve independent Forge publication."
subjects = ["repository:codex-responses-proxy"]
scope = ["VERSION", "CHANGELOG.md", "openspec/changes/archive/2026-08-18-release-2-0-44/**"]
invariants = [
  "version_identity_has_one_owner",
  "gitlab_and_github_remain_independent_release_planes",
  "provider_portable_responses_behavior_is_unchanged",
]
acceptance = [
  "local_quality_and_release_gates_pass",
  "both_provider_release_tags_are_created_natively",
  "installed_runtime_reports_2_0_44",
]
risks = ["one Forge can publish before the other"]
authority_refs = [".ethos/profile.toml", "AGENTS.md", "docs/governance/release-and-change-policy.md"]
predecessors = ["change:runtime-supply-chain-refresh"]
selected_attestations = []
dependencies = []
hypotheses = []
falsifiers = []
experiment_protocols = []
