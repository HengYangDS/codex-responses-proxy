## 1. Release gate repair

- [x] 1.1 Reproduce the GitHub tag-subset failure from the immutable `v2.0.1` checkout.
- [x] 1.2 Add the failing tagged-GitHub execution contract and pass the provider in every test-driver branch.
- [x] 1.3 Record `v2.0.2` and the immutable failed-release status of `v2.0.1`.

## 2. Proof and publication

- [x] 2.1 Run focused release, OpenSpec, presentation, and quality regressions.
- [x] 2.2 Run the complete Python 3.12, 3.13, and 3.14 matrix and HEAD-bound ETHOS proof.
- [x] 2.3 Archive the completed repository change for landing.

Hosted main CI, tag trust, signed Releases, asset parity, installation, runtime,
MCP, original-conversation continuity, and housekeeping are post-land external
acceptance transitions. They remain explicitly unverified in the claim and
chronicle rather than masquerading as incomplete implementation tasks.
