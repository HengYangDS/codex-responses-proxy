## 1. Root-cause regressions

- [x] 1.1 Prove isolated direct watchdog loading and macOS argv identity.
- [x] 1.2 Prove first-install stdout/stderr log paths and directory creation.
- [x] 1.3 Prove no released Python module shadows `select` or `logging`.

## 2. Portable release boundary

- [x] 2.1 Prove authenticated private GitLab asset reads and digest comparison.
- [x] 2.2 Prove the locked `uv` quality environment across hosted and local CI.
- [x] 2.3 Prove the exact prior-payload filename migration and rollback inventory.

## 3. Closeout

- [x] 3.1 Run focused tests, strict OpenSpec, metadata, one full quality gate,
  Python 3.12-3.14 compile, and exact-HEAD proof.
- [x] 3.2 Transfer governed landing, signed dual-Forge 2.0.5 publication,
  equal asset digests, and hosted CI to the post-archive lifecycle without
  claiming those external transitions here.
- [x] 3.3 Transfer signed installation, 8792 health and real-stream proof,
  compaction, retry-limit recovery, and continued original-task dialogue to the
  post-publication lifecycle without claiming runtime recovery here.
- [x] 3.4 Transfer lane retirement and controlled temp cleanup to authorized
  closeout; foreign, dirty, and ownership-unknown lanes remain intact.

## Post-archive lifecycle

Governed landing, hosted CI, signed GitLab and GitHub tags and Releases, asset
parity, signed installation, listener and watchdog identity, configurable-port
proof, real streaming, compaction, retry-limit recovery, unchanged original-task
continuation, and authorized housekeeping are external transitions. Fresh
repository, Forge, runtime, and Codex evidence must establish each one.
