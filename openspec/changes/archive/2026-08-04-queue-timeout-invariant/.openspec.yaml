schema: spec-driven
created: 2026-08-04
goal: Derive the default local queue wait from the total upstream stream
  deadline so a waiter is never denied while the holder ahead of it is still
  inside its own deadline.
