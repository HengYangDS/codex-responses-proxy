schema_version = 1
id = "change:quality-policy-convergence"
intent = "Converge every enforced Proxy repository rule onto one justified product-risk contract and remove obsolete or duplicate vetoes."
subjects = ["repository:codex-responses-proxy"]
scope = [
  "AGENTS.md",
  ".config/checks/**",
  "docs/governance/**",
  "openspec/changes/quality-policy-convergence/**",
  "openspec/specs/**",
  "tools/**",
  "tests/**",
]
invariants = [
  "protocol correctness security process ownership portability and evidence authenticity remain protected",
  "every merge veto has one semantic owner and a documented satisfiable risk model",
  "presentation and maintainability heuristics cannot impersonate product correctness",
  "repository-native verification remains deterministic across supported Python and host platforms",
]
acceptance = [
  "obsolete contradictory negative and duplicate rule surfaces are removed",
  "remaining quantitative vetoes define measurement semantics false-positive cost remediation and review conditions",
  "coverage owns aggregate and semantic-package risk rather than file-size artifacts",
  "the complete repository quality behavior matrix and release proof graph passes from the locked product toolchain",
]
risks = [
  "removing a stale rule may reveal a real product invariant that must be restated positively",
]
authority_refs = [
  "user_instruction",
  "AGENTS.md",
  ".ethos/profile.toml",
  ".config/checks/**",
  "openspec/specs/quality-boundaries/spec.md",
]
hypotheses = []
dependencies = []
