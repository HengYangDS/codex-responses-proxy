## 1. Portable predecessor download

- [x] 1.1 Add a contract that rejects POSIX-only predecessor expansion and
      verify that it fails against the previous workflow.
- [x] 1.2 Consume the existing predecessor value through GitHub Actions
      expression interpolation and verify the focused workflow contract passes.
- [x] 1.3 Regenerate the GitHub workflow from CUE and verify projection, CUE,
      and Action syntax checks pass.

## 2. Closeout

- [x] 2.1 Run the affected repository gates and strict OpenSpec validation.
- [x] 2.2 Archive this locally proven Change before hosted publication consumes
      the final source commit.
