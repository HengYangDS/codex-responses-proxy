# Design

Keep the existing quality owner and test class. Rename only the two uncollected
methods to pytest's public discovery grammar and replace the stale literal with
the repository's current 110-ELOC contract. No helper, compatibility layer, or
second policy owner is introduced.
