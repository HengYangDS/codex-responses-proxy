# Responses protocol integrity closeout evidence

Date: 2026-08-01

## Observed gaps

Read-only inspection of `1fa8d71f23454c99e886c3256ffee7b31cd7bc74`
showed that non-stream Responses bypassed the SSE ciphertext projection, empty
and incomplete HTTP 2xx bodies could be committed as success, an empty request
body bypassed sanitizer admission, and provider route suffixes were broader
than the exact Responses endpoint owned by the product.

## Active change boundary

`openspec/changes/responses-protocol-integrity-closeout` is the sole active
change for the repair. It binds regression-first implementation, fail-closed
request and response boundaries, provider-name-free wire policy, complete local
and hosted gates, release asset parity, transactional installation,
multi-provider runtime proof, PyCharm MCP revalidation, and same-original-
conversation Codex continuity.

This initial evidence binds the observed defect and change intent only. It does
not claim implementation, CI, publication, deployment, runtime recovery, or
housekeeping completion. No Codex JSONL, SQLite, historical message,
Responses-item record, conversation model, or model metadata was read or
modified for this repair.

## Local candidate verification

The current uncommitted candidate was verified on 2026-08-01 with the
repository-owned latest-stable quality dependency SSOT:

- `coverage==7.15.2`, `ruff==0.16.1`, and `ty==0.0.65`, independently checked
  against the official PyPI package metadata;
- GitHub Actions `actions/checkout@v7.0.1` and
  `actions/setup-python@v7.0.0`, pinned to their official tag commit objects;
- CPython `3.12.13`, `3.13.14`, and `3.14.6`, verified as the newest patch tags
  in each supported upstream series, with matching official
  `library/python:<version>-slim` container tags available;
- stable GitHub-hosted runners `macos-26`, `ubuntu-24.04`, and `windows-2025`;
  the newer Ubuntu 26.04 image remained preview and was therefore not admitted;
- the complete 50-file canonical test inventory passed with compilation and
  warning-as-error enforcement on Python 3.12.13, 3.13.14, and 3.14.6;
- Ruff, formatting, portability, repository architecture, ty, and all 50
  coverage-bearing test files passed without an unhandled traceback, warning,
  error banner, failure, or skip;
- statement coverage was `5286/5422 = 97.49%` and branch coverage was
  `1584/1664 = 95.19%`, both strictly above 95%; and
- `openspec validate --all --strict --json` passed all four items, while the Git
  diff contained no Codex application-private session or model-data path.

Release metadata, Markdown, both GitHub workflow contracts, both provider tag
contracts, and the GitLab publication contract also passed locally. These
results bind the mutable candidate tree only; a commit changes the evidence
identity and therefore requires exact-HEAD proof. Hosted CI, signed Forge
history, publication, installation, provider runtime, PyCharm MCP,
same-original-conversation continuity, and lane housekeeping remain unproved.
