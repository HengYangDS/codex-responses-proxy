# Linux branch coverage closeout evidence — 2026-08-03

## Facts

- GitLab pipeline `4064` at the accepted `2.0.7` candidate failed only job
  `29322`, `verify-python-quality`: independent branch coverage was 1640/1728,
  or 94.91 percent.
- The two remaining uncovered outcomes were the Darwin default state and log
  roots and fail-closed rejection of an incomplete native argv payload. Both
  are deterministic contracts behind injected platform or synthetic `ctypes`
  boundaries; neither requires a Linux process to call Darwin `sysctl`.
- The table-driven repository path test now covers the Darwin defaults, and the
  existing synthetic native-payload test now covers the incomplete vector.
- Focused repository and process suites pass. The clean Linux Python 3.12
  quality gate passes with 96.78 percent combined coverage and 1642/1728, or
  95.02 percent, independent branch coverage.
- The complete behavior inventory and isolated compilation pass on Python
  3.12, 3.13, and 3.14.
- No production runtime code changes in this closeout. Signed `v2.0.5` and
  `v2.0.6` tags and their failed hosted runs remain immutable; `v2.0.7` is the
  forward-only publication candidate.
- No Codex JSONL, SQLite, history, title, archive, model metadata, AIGW state,
  or JetBrains state is read or changed.

## Evidence

- Linux Python 3.12 quality: `/private/tmp/proxy-v207-linux-quality-fix2.log`.
- Python 3.12-3.14 matrix: `/private/tmp/proxy-v207-matrix-fix.log`.
- Clean Linux verification container:
  `3c7b7776e3b8ed5bac46ed5df705df4872cfb84fd6daab0a4fc33cd55d4c1b15`
  exited zero.

These workstation-local paths identify diagnostic inputs, not publication
proof.

## Unverified

- Final commit identity and HEAD-bound ETHOS proof remain pending.
- Both Forge main and tag pipelines, signed `v2.0.7` tags, Releases, and asset
  identity remain pending.
- Transactional installation, live provider acceptance, containment removal,
  unchanged original-task continuation, and lane housekeeping remain pending.

## Next step

Archive this complete repository change, sign the candidate, execute exact-HEAD
proof, and land it before independent Forge publication and installation.
