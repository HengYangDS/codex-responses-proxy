# Route-scoped backpressure evidence — 2026-08-03

## Facts

- The installed 2.0.4 runtime allowed eight simultaneous Responses requests
  across all routes and checked provider cooldown before admission only.
- A burst of concurrent Codex tasks targeted the UCloud route. HTTP 429 was
  already terminal after one upstream attempt, but requests admitted before the
  first 429 remained able to reach the same provider.
- At 2026-08-03 01:07:19 +08:00 the installed runtime was contained by setting
  its global Responses concurrency to one. This is a safe temporary operating
  state, not the released terminal design.
- From that restart through 2026-08-03 02:07:16 +08:00, the live log recorded
  247 sanitized requests, 246 completed streams, zero new upstream HTTP 429
  events, and zero local queue timeouts. One stream EOF remains a separate
  reliability observation and is not classified as a provider rate limit.
- The target design keeps the global bound, serializes only the same provider
  route, permits different routes to overlap, and rechecks cooldown after route
  admission and before provider I/O.
- The focused RED failed because route identity was absent from admission and
  cooldown was checked only before queuing. The focused GREEN then passed all
  three contracts, followed by 32 complete admission and transport tests.
- Strict candidate verification passed release metadata, Markdown, 42 focused
  tests, compilation, and diff hygiene. The complete repository gate then
  passed from its locked Python 3.12 `.venv` with no traceback, warning, or
  error banner; combined coverage was 96.78 percent and branch coverage was
  1642/1728, or 95.02 percent.
- The complete behavior inventory and isolated compilation passed on distinct
  Homebrew Python 3.12.13, 3.13.14, and 3.14.6 runtimes. Strict OpenSpec
  validation passed all four items.
- The final atomic candidate matrix exited zero after release metadata,
  Markdown, strict OpenSpec, and all three interpreter-bound behavior suites.
  Its bounded housekeeping removed 42 generated bytecode files, proved no
  bytecode residue remained outside `.venv`, and preserved the candidate
  content digest `2377c5de0bfdb5f4ae13647cb6fd1bc1502aadc61eaf0f0e91805dfae55eb48a`.
- No Codex JSONL, SQLite, history, archive, or model metadata is read or changed
  by this repair.

## Evidence

- Containment switch log:
  `/private/tmp/proxy-429-mitigation-switch-20260803T0106.log`.
- Runtime observation:
  `/private/tmp/proxy-429-mitigation-observation.json`.
- Latest containment status:
  `/private/tmp/proxy-429-mitigation-latest-status.json`.
- Focused RED:
  `/private/tmp/proxy-route-backpressure-red-focused.log`.
- Focused GREEN:
  `/private/tmp/proxy-route-backpressure-green-focused.log`.
- Complete admission and transport GREEN:
  `/private/tmp/proxy-route-backpressure-green-related.log`.
- Strict focused release candidate gate:
  `/private/tmp/proxy-v206-focused-strict.log`.
- Warning-free locked quality and coverage gate:
  `/private/tmp/proxy-v206-quality-python312-clean.log`.
- Interpreter identity, strict OpenSpec, complete Python matrix, generated-file
  housekeeping, and terminal receipt:
  `/private/tmp/proxy-v206-final-matrix-pass.log` (`FINAL_MATRIX_OK`, exit 0).

These workstation-local paths are diagnostic inputs, not repository-published
proof. RED/GREEN, full gates, signed publication, installation, live provider
acceptance, and original-task continuation will be recorded as they occur.

## Unverified

- Version 2.0.6 has not yet been built, published, installed, or accepted.
- Hosted CI, Forge, installed-runtime, and exact original-task evidence remain
  pending.
- Provider-side quota details remain unknown and are not encoded in product
  behavior.

## Next step

Freeze and sign the verified candidate, execute HEAD-bound ETHOS proof, then
complete the independent Forge, installation, runtime, and unchanged-task
evidence planes.
