# Exact v2 current-install migration evidence

Date: 2026-08-02

## Facts

- The installed v2.0.0 protocol-v2 payload has 57 manifest-owned runtime files,
  a canonical release receipt, and no `release-install-state.json`.
- The pre-repair v2.0.3 installer rejected that exact projection before payload
  mutation as an unsupported retired schema-2 file set.
- Source commit `d1f586903ee4e46c2b3caa82ce80f268ca7fbae9` models the complete
  v2.0.0 runtime and serving inventories, verifies every file digest, serving
  aggregate, receipt digest, canonical receipt, receipt release, and exact
  entrypoint, and strictly verifies finalized install state when it exists.
- Only the explicitly modeled v2.0.0 shape may omit finalized install state.
  Rollback restores `replay/event.py`, removes the successor
  `replay/response.py`, restores the original receipt, and preserves the
  original absence of finalized state.
- `runtime.config.DEFAULT_PORT` is the single production literal for the 8792
  default. Installer, control, and uninstall `--port` arguments plus
  `CODEX_RESPONSES_PROXY_PROXY_PORT` continue to select any valid TCP port.
- The exact live v2.0.0 install root passed read-only historical admission with
  release `2.0.0`, 57 files, metadata limited to the manifest and receipt, and
  `codex_responses_proxy/replay/event.py` as the sole current-retired path.
- Strict OpenSpec validation, release metadata, Markdown, focused migration and
  negative tests, the complete repository quality gate, and the Python 3.12,
  3.13, and 3.14 compile plus behavior matrix passed locally. The quality gate
  covered 5,530 statements and 1,704 branches with no threshold gap.

## Evidence

- Full quality log:
  `/private/tmp/codex-responses-proxy-v2.0.4-quality-full-3.log`.
- Python matrix logs:
  `/private/tmp/codex-responses-proxy-v2.0.4-python-3.12.log`,
  `/private/tmp/codex-responses-proxy-v2.0.4-python-3.13.log`, and
  `/private/tmp/codex-responses-proxy-v2.0.4-python-3.14.log`.
- The first exact-HEAD ETHOS execution retained evidence digest
  `3f57e63a2d9947284eb8dd079cb96be3d42c02dc645db6aaa488d0ac8471c965`;
  it proved the platform matrix and exposed only missing claim/scope binding
  plus a host-global coverage-version mismatch. The repository quality gate
  itself passed in the requirements-locked isolated environment.

These `/private/tmp` paths are workstation-local diagnostic inputs, not
published repository evidence. This Chronicle retains only bounded identities,
counts, command outcomes, and paths to local receipts.

## Unverified

- Final exact-HEAD ETHOS proof, OpenSpec archive, candidate/accepted/release-root
  landing, hosted CI, signed dual-Forge publication, release assets, installed
  v2.0.4 runtime identity, and same-original-task continuation remain separate
  transitions until freshly verified.
- Codex Desktop latest-stable status and remaining workstation housekeeping are
  not established by this repository evidence.

## Boundary

No Codex JSONL, SQLite, transcript, historical message, title, archive state, or
model metadata was read or modified by this change.
