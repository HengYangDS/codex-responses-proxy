# Three-provider compaction acceptance — 2026-08-02

## Facts

- Release 2.0.4, accepted source `066f86f`, and the installed payload all
  contain the fail-closed `compaction_trigger` projector.
- The unchanged original conversation
  `019f477e-7aaf-7fd0-837b-41606d7a8b5f` and the two recent diagnosis
  conversations continued successfully through UCloud after installation.
- The governed provider registry contains exactly DMXAPI, UCloud, and AIHubMix.
- This increment binds the existing compaction control to all three route
  namespaces and corrects the current canonical naming; it does not modify the
  runtime implementation.

## Evidence

- Source ancestry and installed-file inspection prove the 2.0.4 projector is
  present in accepted, tagged, and live payload planes.
- Read-only session-tail inspection shows successful post-installation replies
  in all three target conversations.
- The provider route regression captures the three outbound request bodies and
  requires byte equality plus the exact payload-free trigger.

## Unverified

- AIHubMix live upstream acceptance remains pending until a fresh AIGW switch
  and minimal request complete.
- DMXAPI live upstream acceptance remains unavailable while its quota is
  exhausted; only local route/projection acceptance may be claimed.

## Boundary

No Codex JSONL, SQLite, transcript, title, archive, model metadata, credential,
or AIGW configuration is edited by this source change. Publication,
installation, and live-provider observations remain separate truth planes.
