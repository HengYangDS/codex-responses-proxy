# DMXAPI replay reliability closeout

Date: 2026-07-31

## Initial failure and repair boundary

The initially installed `v1.0.44` payload passed its 37-file integrity check and
served through one accepting listener with AIGW route authority. That runtime
health did not prove replay continuity.

The bounded, content-free proxy log exposed repeated normal request projections
ending locally as
`provider_portable_projection_rejected reason=empty_text_content`.
Current-process events were separated from predecessor-process history before
the defect scope was admitted.

The log retains route, reason, request sequence, path, counters, and timestamps;
it does not retain request text, tool arguments, output values, credentials,
item IDs, or ciphertext.

## Root-cause boundary

The normal projector validates a call-output pair and then sends its output
through the same strict input-content projector used by ordinary messages. An
exact empty string is consequently rejected before upstream I/O even when the
call kind and `call_id` relationship are complete.

This is a request-copy transformation. No repair requires or authorizes edits
to Codex JSONL, SQLite, transcript history, archives, resume pointers, or model
metadata. AIGW continues to own provider configuration, credentials, endpoint
selection, and provider switching.

## Source and quality evidence

The source repair gives only a structurally valid paired empty
function/custom-tool result the fixed marker `[tool returned no textual
output]`. Empty ordinary dialogue, missing or null output, unknown content, bad
pairing, and duplicate output remain fail-closed. The implementation adds no
dependency, facade, forwarding module, flat compatibility package, or
configuration surface.

Fresh local checks on the committed release tree passed:

- the focused provider-portable suite on Python 3.12, 3.13, and 3.14;
- the repository quality gate with Ruff, formatting, structural and public
  docstring audit, `ty`, all 440 tests, 96.94% statement coverage, and 95.25%
  branch coverage;
- the independent compile-and-behavior matrix with the same 440 tests on each
  supported Python version and no traceback, ignored exception, SocketServer
  banner, or Python warning;
- release preparation and chronology for `v1.0.45`, Markdown presentation,
  diff hygiene, and strict OpenSpec validation with four of four items valid
  and no diagnostic issue.

The OpenSpec archive dry-run was clean after removing the premature canonical
spec double-write. The local source lineage through
`8176405a9899218cf05029c38d01b92462bc1b01` has tree
`a5c3e878139e7d817b0649fca04364a38ba3722c` and uses the GitLab identity
`Yang HENG <heng.yang.ds@hotmail.com>`.

## Dual-Forge release evidence

The signed `v1.0.45` tag and non-draft release exist on both Forge planes. The
GitLab release commit is `a627de86490f0b9bd549450509a45573a581656c`;
the GitHub release commit is `0d59be52a41549034a134701e47c0f9ba6a131b1`.
Provider-native commit and tag object IDs differ, but both release commits have
tree `a5c3e878139e7d817b0649fca04364a38ba3722c`.

The GitLab pipeline recorded all seven release jobs as successful. The GitHub
verify and release runs recorded all nine matrix, quality, governance, tag, and
publish jobs as successful. Fresh publication evidence reported
`tree_equal=true`; each platform reported its release tag signature as valid.
The two source-asset projections were compared by content rather than by
provider-native archive metadata.

## Installed runtime evidence

The release installation completed at `2026-07-31T11:11:30+08:00`. At the
`2026-07-31T11:45:04+08:00` observation, PID `98082` was the sole listener on
`127.0.0.1:8791`, release `1.0.45` was accepting and not draining, and the
installed control plane reported `37 files verified`.

The runtime identity was bound to payload manifest
`5eed795c346ab6be375581118ce8c4159d05a6244279b5ad507daef213f3146a`,
release receipt
`6a6f641aa59b9574ccdc36dd94eedc35e5899ed75b4a7a36dccf7976d4d1019a`,
and serving payload
`55b6519add12b6d2ce2a7a2221b192b34f37016272f5f367ed27030239764464`.
AIGW remained the route authority for the scoped `dmxapi` endpoint.

## Continuity observation and limits

The current user-visible task continued after installation without a new task
or history rewrite. On the same PID, the `11:45:04+08:00` snapshot recorded
`157` completed Responses streams. The empty-response recovery counters,
input-variant recovery counters, local queue timeouts, incomplete streams, and
pre-content exhaustion counters were all zero. No `empty_text_content`, invalid
input, bounded `503`, traceback, ignored exception, SocketServer banner, or
Python warning was observed in the bounded post-installation evidence window.

This is not a zero-failure claim. The process lifetime recorded two failed
streams. The most recent failure was an upstream EOF at
`2026-07-31T11:32:21+08:00`, after which the same process completed further
requests. The upstream classification counters also contain one HTTP `401`
from a `/models` request. Secret-free runtime evidence cannot associate a
request counter with private conversation content; continued interaction in
this task and the listener counters are therefore separate evidence planes.

No step read or changed Codex JSONL, SQLite, transcript history, resume state,
or model metadata. The corrective immutable record and owner-lane retirement
remain outside this dated repository carrier until their own closeout checks
complete.
