# Codex session closeout source evidence — 2026-08-02

## Facts

- The 2.0.4 launchd watchdog direct-script path could resolve sibling module
  names ahead of the standard library and exit before product logging.
- The signed source candidate removes the shadowing names, bootstraps the package
  root before subprocess import, preserves native macOS argv identity, persists
  watchdog standard streams, keeps 8792 as one configurable default, uses a
  locked quality environment, and authenticates private GitLab asset reads.
- Focused supervision regressions and strict OpenSpec validation pass.
- Exact-HEAD quality and Python 3.12-3.14 compilation are separate receipts and
  do not establish publication, installation, or original-task recovery.

## Boundary

No Codex JSONL, SQLite, transcript, archive, model metadata, AIGW route, or
PyCharm configuration is changed. Forge publication, installed runtime,
compaction, retry-limit recovery, same-task dialogue, and lane retirement remain
external until fresh proof.
