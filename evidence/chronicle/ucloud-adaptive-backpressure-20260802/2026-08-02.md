# UCloud adaptive backpressure evidence — 2026-08-02

## Facts

- Original failed Codex thread:
  `019fc0b7-9094-7350-8772-df9a14a47a36`.
- The observed terminal request returned UCloud HTTP 429 after proxy and Codex
  retry layers had amplified provider backpressure; the incident work did not
  read or modify the thread's JSONL, SQLite row, transcript, or model metadata.
- Signed release `v2.0.2` at
  `865db51b01fc962beb4e9e8db0de6fb83759aa27` already owns the one-attempt
  HTTP 429 relay, provider-scoped cooldown, five-second fallback, five-minute
  cap, and default Responses concurrency of 8.
- On that exact base, a 300-second cooldown recorded at monotonic time 10 and a
  later five-second cooldown recorded at time 11 left only four seconds at time
  12. The focused RED test exited 1 with `4.0 != 298.0`.
- The repair stores the later of the active and newly computed deadlines under
  the existing lock. The same focused test then retained 298 seconds, and the
  current cooldown unit suite passed 3/3.
- The current transport regression set passed 59/59 after the repair.
- The canonical Python 3.12 quality gate exited 0 with Ruff, formatting,
  portability, repository, type, full behavior, and branch-coverage checks;
  aggregate branch coverage was 96.94% with no policy gaps.
- The complete compile inventory exited 0 on Python 3.12.13, 3.13.14, and
  3.14.6. Strict OpenSpec validation passed all four current items.
- Exact-HEAD ETHOS proof passed both trust-bearing gates for signed commit
  `0b88c8e35381935cba45c00cbd367b6a68b65cb0`; its evidence digest is
  `e7cebf87c67294a8d34152320f8d68a62ae7c47ad32728e033c4bbd86e963473`.
- Official OpenSpec archive moved the completed change to
  `openspec/changes/archive/2026-08-02-ucloud-adaptive-backpressure` and
  promoted its requirement into the canonical provider-portable spec.

## Evidence

- RED log: `/tmp/ucloud-monotonic-red-current-base.log`.
- GREEN log: `/tmp/ucloud-monotonic-green-current-base.log`.
- Focused regression log: `/tmp/ucloud-monotonic-focused-regression.log`.
- Pre-refresh candidate backup:
  `/tmp/ucloud-old-base-candidate-20260802T092413Z.patch` and matching
  SHA-256 receipt `/tmp/ucloud-old-base-candidate-backup.sha256`.
- Official lane refresh and lease-CAS repair receipts:
  `/tmp/ucloud-refresh-base-apply.json` and
  `/tmp/ucloud-lease-head-committed.json`.
- Canonical quality receipt and log:
  `/tmp/ucloud-final-quality-2.0.3-python312.receipt` and
  `/tmp/ucloud-final-quality-2.0.3-python312.log`.
- Compile matrix receipts:
  `/tmp/ucloud-final-compile-2.0.3-python312.receipt`,
  `/tmp/ucloud-final-compile-2.0.3-python313.receipt`, and
  `/tmp/ucloud-final-compile-2.0.3-python314.receipt`.
- Exact-HEAD proof receipt and result:
  `/tmp/ucloud-final-ethos-proof-2.0.3-isolated.receipt` and
  `/tmp/ucloud-final-ethos-proof-2.0.3-isolated.json`.

The `/tmp` paths are workstation-local diagnostic inputs, not published
repository evidence. This Chronicle retains only bounded identifiers, source
heads, counts, and command outcomes.

## Unverified

- Hosted CI, signed GitLab and GitHub publication, release assets, installation,
  listener identity, and unchanged-thread continuation remain external.
- The UCloud account's exact service-side quota remains unknown and is not
  encoded by this change.

## Next step

Complete source proof, land the narrow 2.0.3 candidate after the active train,
publish and install through the signed protocol-v2 lifecycle, then resume the
unchanged original thread for multiple successful turns.
