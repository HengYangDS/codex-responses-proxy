# Hosted Darwin coverage evidence — 2026-08-03

## Facts

- GitLab pipeline `4062` at source commit
  `6f037b18932d75606ac18c4eb3d520bce5635831` failed only
  `verify-python-quality`: combined coverage was 96.57 percent but independent
  branch coverage was 1636/1728, or 94.68 percent.
- The missing successful lines belonged to Darwin `kern.procargs2` decoding.
  The real subprocess integration was correctly restricted to Darwin after
  `v2.0.5` had called a nonexistent Linux `sysctl` symbol.
- A synthetic valid native payload now exercises the successful decoder on
  every supported host without pretending that Linux implements Darwin
  `sysctl`.
- The focused process suite passes 11 tests. The locked Python 3.12 quality gate
  passes with 96.78 percent combined coverage and 1642/1728, or 95.02 percent,
  independent branch coverage.
- The complete behavior inventory and isolated compilation pass on Python
  3.12, 3.13, and 3.14.
- Signed `v2.0.6` tags and failed hosted evidence remain immutable. Version
  `2.0.7` is the forward-only candidate.
- No production runtime code, AIGW state, JetBrains state, or Codex JSONL,
  SQLite, history, title, archive, or model metadata is changed.

## Evidence

- GitLab main failure: pipeline `4062`, job `29310`.
- Hosted failure trace:
  `/private/tmp/proxy-v206-gitlab-main-quality-failure.log`.
- Focused suite: `/private/tmp/proxy-v207-focused2.log`.
- Full quality: `/private/tmp/proxy-v207-quality2.log`.
- Python matrix: `/private/tmp/proxy-v207-matrix.log`.

Workstation-local log paths are diagnostic inputs, not publication proof.

## Unverified

- Final commit identity and HEAD-bound ETHOS proof remain pending.
- Both Forge `v2.0.7` tags, hosted CI, Releases, and asset identity remain
  pending.
- Installation, live provider acceptance, containment removal, and unchanged
  original-task continuation remain pending.

## Next step

Bind this evidence to the signed candidate, execute exact-HEAD proof, land it,
then complete independent Forge publication before any installation.
