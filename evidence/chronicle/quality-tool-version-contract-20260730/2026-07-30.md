# Quality tool version contract observation

Date: 2026-07-30

The immutable `v1.0.39` GitHub quality execution installed `ty 0.0.64`, which
reported `ty 0.0.64 (5e64a131b 2026-07-27)`. The repository owner compared the
whole display line and rejected the correct pinned release.

The repair defines identity as the exact tool name and semantic version, with
only an optional space-delimited informational suffix. Behavioral tests prove
that metadata-bearing output is accepted and `ty 0.0.640` is rejected. The
real PyPI `ty==0.0.64` package passed the canonical quality gate at 96.94%
statement/combined coverage and 95.01% branch coverage. Strict OpenSpec
validation and the focused quality contract suite also passed.

This is local evidence only. Hosted CI, provider-native publication,
installation, runtime identity, and repository-family closeout remain separate
transitions.
