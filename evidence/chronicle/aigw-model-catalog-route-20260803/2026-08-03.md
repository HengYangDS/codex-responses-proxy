# AIGW model-catalog route evidence — 2026-08-03

## Facts

- Fresh `aigw check` reached the configured loopback service and received a
  local HTTP 404 with `provider_route_not_found`; it did not establish an
  incorrect DMXAPI endpoint suffix or a `.cn`/`.com` account mismatch.
- Fresh `aigw catalog` reported a catalog request failure for AIHubMix, DMXAPI,
  and UCloud. Each account targets a provider-scoped loopback base of the form
  `http://127.0.0.1:8792/<provider>/v1`.
- The listener on `127.0.0.1:8792` is the installed 2.0.7 projection. It
  accepts provider-scoped Responses traffic but closes
  `GET /<provider>/v1/models`, so AIGW discovery fails locally before a
  provider catalog is contacted.
- The 2.0.8 source candidate admits only exact read-only
  `GET /<provider>/v1/models` for `dmxapi`, `ucloud`, and `aihubmix`. It sends
  one authenticated upstream request and relays status, eligible headers, and
  body. It neither reads a catalog request body nor enters Responses replay,
  admission, cooldown, retry, or recovery policy.
- The candidate does not change AIGW configuration or credentials, Codex JSONL,
  SQLite state, archives, or conversation model metadata. The OpenSpec change
  was archived as `2026-08-04-aigw-model-catalog-route` after strict validation.

## Evidence

- Fresh runtime symptom: `aigw check`, `aigw catalog`, and loopback
  `GET /dmxapi/v1/models` on the installed 2.0.7 listener returned the local
  closed-route 404.
- Focused route and ownership contracts passed: 61 tests via
  `python3.14 -m unittest tests.transport.test_input tests.providers.test_registry tests.providers.test_routes tests.governance.test_owner_boundaries -v`.
- Locked quality passed with 96.77% total statement coverage and 95.09% total
  branch coverage via `PYTHON=python3.12 RUFF=ruff TY=ty sh tools/quality/run.sh`.
- Python 3.12, 3.13, and 3.14 completed the repository compile/test inventory
  through `tools/quality/tests.py --compile`.
- Release metadata and Markdown checks passed for 2.0.8; strict OpenSpec
  validation passed before archival.

## Unverified

- The candidate is not yet committed, landed, published, tagged, installed, or
  accepted by live AIGW calls.
- Provider-side catalog contents, quota, and account health are not inferred
  from this local routing repair.
- A user-visible response in any historical Codex conversation remains a
  separate acceptance claim.

## Next step

Commit and prove the exact source head, complete governed dual-Forge
publication and transactional installation, then rerun `aigw check`,
`aigw catalog`, `aigw verify`, and one DMXAPI Responses request against the
released runtime.
