# Release gate contract repair observation

Date: 2026-07-30

The immutable `v1.0.37` tag executions failed because
`scripts/test_release_metadata.py` still required the old ty version-check shell
pattern after the quality owner had moved to exact version selection across
`PATH`. GitLab dependency bootstrap also emitted debconf frontend fallback
warnings before the release contract failed.

The repair moves the behavior regression to the quality-test owner, keeps
release tests focused on provider projections, and makes every GitLab Debian
bootstrap explicitly noninteractive and quiet. Local verification passed the
release and presentation contracts, strict OpenSpec validation, the Python
3.12/3.13/3.14 matrix, and the canonical quality gate at 96.94% combined and
statement coverage with 95.01% branch coverage. Local logs contained no
traceback, ignored exception, socketserver banner, Python warning, pip root
warning, or debconf frontend warning.

This observation is local evidence only. Hosted CI, provider-native tags and
Releases, installation, runtime identity, and repository-family closeout remain
separate transitions.
