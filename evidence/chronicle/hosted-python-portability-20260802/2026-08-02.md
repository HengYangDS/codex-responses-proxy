# Hosted Python portability evidence

Date: 2026-08-02

GitHub run `30735834625` reached the Windows 2025 Python setup step but could
not resolve the repository-pinned `3.12.13` build. The failure occurred before
tests, while the same source had already passed local Python 3.12, 3.13, and
3.14 verification. This proves a hosted supply-surface mismatch, not a product
behavior failure.

The candidate replaces hosted patch pins with the supported Python release lines
`3.12`, `3.13`, and `3.14` on both Forge projections. Repository contracts reject
future `3.12.x`, `3.13.x`, or `3.14.x` hosted-CI pins. The complete supported
interpreter matrix passed at exact HEAD
`b41c09caf8fbed851122bd7cab34223da0cff732`. The Python quality gate had passed
on the preceding product commit with 96.94% statement and branch coverage; a
later ETHOS proof invocation used a temporary environment whose `coverage` was
downgraded from the required 7.15.2 to 7.15.0, so that invocation is not accepted
as final exact-HEAD quality proof and must be rerun with the repository-owned
tool versions.

These are local and observed-hosted-failure facts only. A green hosted run at the
final projected tip, publication, installation, runtime validation, PyCharm MCP,
original-conversation continuity, and housekeeping remain external acceptance
steps. No Codex JSONL, SQLite, historical message, Responses-item data, or model
metadata was read or modified.
