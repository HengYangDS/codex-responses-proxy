# Release pipeline closeout evidence — 2026-08-02

## Facts

- GitHub's native `v2.0.1` checkout reproduces the metadata-test traceback when
  the already-tagged branch invokes the checker without `--provider github`.
- Passing the provider in that branch makes the same real tag-subset fixture
  complete with zero traceback, warning, failure, or skip.
- GitLab chronology and exact-tag validation are unchanged.

## Evidence

- Failing fixture: GitHub `v2.0.1` tag checkout with its provider-native tags.
- Passing command: `GITHUB_ACTIONS=true GITHUB_REF_TYPE=tag
  GITHUB_REF_NAME=v2.0.1 python3 tests/release/test_metadata.py`.
- OpenSpec validation: `openspec validate release-pipeline-closeout --strict
  --json`.
- The first aggregate gate used a non-fail-fast shell and therefore did not
  prove success; that observation is explicitly rejected rather than promoted.
- Strict fail-fast verification then completed with exit code 0 across release
  metadata, Markdown, OpenSpec, canonical quality and coverage, and the Python
  3.12/3.13/3.14 compile-test matrix.
- Strict-gate log SHA-256:
  `58bb36d78597c85edfff8dabf08aae92fc783ce8456fab74a1e813ebe747488f`.
- Its diagnostic scan contains zero traceback, warning, failure, error, or skip
  markers.

## Unverified

Exact-HEAD proof, both hosted main and tag pipelines, external trust inputs,
Releases, asset parity, installation, runtime behavior, MCP,
original-conversation continuity, and repository-family housekeeping.

## Next step

Complete the release metadata, full proof, forward-only `v2.0.2` publication,
installation, runtime acceptance, and owner-bound closeout.
