# Loopback DNS-independent admission evidence

Date: 2026-08-02

## Observed defect

GitHub Actions run `30736936904` reached the macOS Python 3.12, 3.13, and
3.14 test jobs, but listener construction printed only its start marker and
then each test reached the 30-second timeout. The same source passed Windows,
GitLab, governance, and local product gates. Focused tracing isolated the stall
to the inherited HTTP server bind path calling hostname resolution before the
socket began listening.

## Repair boundary

Source commit `3dad427c2ca1dc8db466a3a466f48f60798c6204` binds the loopback
socket through the transport server primitive and derives the presented host
and port from the kernel-bound address. Fresh and protocol-v2 handoff-adopted
listeners follow the same rule. A focused regression makes hostname resolution
raise and proves both listener forms still construct with the exact bound
identity.

## Local verification

The source commit passed the complete Python 3.12, 3.13, and 3.14 behavior
matrix. The repository quality gate passed Ruff, formatting, portability,
structure, type, documentation, tests, clean diagnostics, and statement plus
branch coverage at 96.94%. The active OpenSpec change then passed strict
validation, and the focused listener regression passed on Python 3.14.

These are local source and test facts. The final governance commit, exact-HEAD
ETHOS proof, archive, candidate and accepted-root landing, hosted CI,
publication, installation, provider runtime, 429 runtime containment, PyCharm
MCP, original-conversation continuity, and housekeeping remain separate
transitions until freshly verified.

No Codex JSONL, SQLite, historical message, response-item data, or model
metadata was read or modified.
