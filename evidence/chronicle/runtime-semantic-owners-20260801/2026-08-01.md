# Runtime semantic owners evidence

Date: 2026-08-01

## Observed defect

The frozen source tree at `96f3dbcffa553fd4d2c418d6059ac0636df715aa`
kept logging, telemetry, admission/drain, and empty-response cooldown in one
`runtime/state.py` module. `sanitize_responses_body` returned diagnostic prose,
and runtime telemetry parsed that prose back into counters.

## TDD boundary

The first focused execution of `python3 tests/governance/test_owner_boundaries.py`
failed because `runtime/state.py` still existed and `ProjectionResult` did not.
This record binds local verification to the source-and-test candidate below.
It does not claim hosted CI, publication, installation, provider runtime, MCP,
or original-conversation acceptance.

## Implemented candidate

The mixed module is retired without a facade. Concrete ownership is now:

- `runtime/admission.py`: handler/request admission and bounded drain leases;
- `runtime/telemetry.py`: secret-free counters, failure classes, and status;
- `runtime/logging.py`: redaction, safe labels, bounded retention, and output;
- `transport/cooldown.py`: provider-neutral bounded failure cooldown.

Replay returns immutable `ProjectionResult` and `ProjectionMetrics` values.
Telemetry consumes numeric fields directly. Payload inventory, callers, tests,
package declarations, architecture documentation, and the repository quality
gate now use these direct owners.

Focused GREEN evidence on the staged candidate:

```text
python3 -m unittest tests.runtime.test_admission tests.runtime.test_logging \
  tests.runtime.test_telemetry tests.transport.test_cooldown \
  tests.governance.test_owner_boundaries -v
22 tests passed

python3 -m unittest tests.transport.test_input -v
15 tests passed

python3 -m unittest tests.transport.test_proxy -v
18 tests passed

python3 -m unittest tests.runtime.handoff.test_protocol \
  tests.runtime.handoff.test_runtime -v
47 tests passed

python3 -m unittest tests.quality.test_contract \
  tests.governance.test_owner_boundaries -v
47 tests passed

python3 tools/quality/repository.py
repository_quality_ok=true; gaps=0; files=136
```

Complete local verification froze `HEAD`
`96f3dbcffa553fd4d2c418d6059ac0636df715aa` and index tree
`ca38de296737e0b16f6a7eed4e250e09c841362b` before execution:

```text
PYTHON=/opt/homebrew/bin/python3.12 RUFF=ruff TY=ty sh tools/quality/run.sh
exit 0
statements: 5221/5359 = 97.42%
branches:   1560/1640 = 95.12%
combined coverage report: 96.89%
repository quality: ok=true; gaps=[]
no unhandled traceback, warning, failure banner, or skipped test

for py in /opt/homebrew/bin/python3.12 \
          /opt/homebrew/bin/python3.13 \
          /opt/homebrew/bin/python3.14; do
  "$py" tools/quality/tests.py --compile
done
Python 3.12.13: exit 0
Python 3.13.14: exit 0
Python 3.14.0:  exit 0
```

The subsequent Chronicle, Claim digest, and OpenSpec lifecycle edits are
governance-only projections. The signed final commit and exact-HEAD ETHOS proof
remain the authority for the complete repository tree.

No Codex JSONL, SQLite, transcript, response-item data, or model metadata is in
scope or modified.
