# terminal-product-closeout — 2026-08-04

- Rebased the terminal reconstruction onto the accepted v2.0.10 source and retained the exact model-catalog route while removing ordinary-request serialization and proxy-owned concurrency ceilings.
- Reconstructed production ownership into `cli`, `protocol`, `providers`, `relay`, `service`, and `lifecycle`; declaration-only packages do not re-export peer behavior.
- Added a content-sensitive worktree fingerprint binding HEAD, paths, file type, executable mode, tracked and untracked bytes while excluding Git-internal state.
- Replaced direct test-script execution with pytest-only metadata verification and corrected lifecycle/service documentation to the current owners.
- Reduced `tools/quality/repository.py` to the 600 ELOC hard limit without raising the limit or weakening fingerprint semantics.
- Exact-HEAD quality on commit `04f1f80cf5d960c7392c8d5f9f7ffc92e08948d5` and fingerprint `bf8aa99df655218257b51f5fbe67c297c2b486fd120149f3bc1e26842c94e9e9` passed 580 tests with 96.97 percent combined coverage and branch coverage strictly above the 95 percent floor.
- Python 3.12, 3.13, and 3.14 each passed all 580 tests from built wheels and native executable fixtures; ETHOS subsequently returned `state=proven` with no required gaps for that exact head.
- The release session exposed and then fixed a repeatability defect where its no-Python PATH directory caused `FileExistsError` and a Nox traceback on the second black-box command. The repaired release gate built the 2.0.11 macOS arm64 executable, passed 10 black-box CLI tests without Python on PATH, and produced the archive, manifest, and checksums.
- Local evidence does not prove hosted CI, Forge publication, installed 2.0.11 runtime, live provider success, same-original-task continuation, PyCharm MCP non-regression, or repository-family housekeeping. Those remain explicit downstream tasks.
- The source change was archived as `openspec/changes/archive/2026-08-04-terminal-product-closeout`; the active claim continues to own the explicitly unproved delivery and runtime obligations after governed landing.
- GitLab pipeline `4489` remained pending until the existing Runner 35 was
  restored; its first real execution then failed every native-build job because
  Debian slim did not contain PyInstaller's required `objdump`. The focused
  `linux-native-ci-prerequisites` change declares `binutils` in both affected
  GitLab projections and protects that dependency with release-metadata
  contracts. Hosted success remains unproved until a new exact-head pipeline
  completes.
- Exact-head local verification for commit
  `63686e0361fedad722bb594a26dfed76318e0097` passed quick, quality, Python
  3.12/3.13/3.14, release, and strict OpenSpec gates. Quality passed 580 tests,
  combined coverage remained 96.97 percent, strict branch coverage reported
  1488 of 1564 branches with no gaps, each Python line passed 580 tests, and
  the native release accepted 10 black-box tests. The bounded receipt is
  `/private/tmp/proxy-exact-head-63686e0361fedad722bb594a26dfed76318e0097-20260804T182847Z.log`.
- The Linux prerequisite carrier was archived as
  `openspec/changes/archive/2026-08-05-linux-native-ci-prerequisites` after
  strict validation. The remaining unchecked hosted-CI task is intentionally
  retained in that immutable archive and remains owned by this active claim;
  archival does not assert that the next pipeline is green.
- The archive task transfer explicitly binds candidate/accepted landing and the
  next exact-head GitLab pipeline to this claim. These external transitions are
  still pending and must be evidenced before release publication resumes.
- Accepted `dev` advanced to `64ee0c1c04f102d4b2140a6a0fae5769d43911f7` through governed closeout and passed a fresh accepted-root proof after precise removal of ignored retired Python cache roots. Restoring the unchanged signed `v2.0.8` and `v2.0.9` tag objects then exposed a fail-closed pre-push defect: annotated tag object IDs were treated as commit proof heads. Active change `annotated-tag-push-admission` now owns the forward-only hook contract; no remote tag write occurred while the guard was inconsistent.
- Commit `fdb97868921d40944ca40423dc63422a43d5277c` passed strict OpenSpec validation and a fresh exact-head ETHOS proof with `python-matrix` and `python-quality` both proven and no required gaps. The official Python 3.12, 3.13, and 3.14 sessions each passed all 581 tests. The remaining landing and historical-tag publication transition stays outside this local source proof and remains owned by the active claim.
- The validated change was archived as `openspec/changes/archive/2026-08-04-annotated-tag-push-admission`. Its unchecked landing task is retained truthfully: archive movement does not prove candidate or accepted landing, and the active claim continues to own that transition.
- Repeated exact-head runs exposed a non-deterministic verification coupling: Python matrix and quality sessions each rebuilt a PyInstaller onefile executable and used its cold-starting native payload for real handoff integration, while the release session repeated the same native proof. Change `verification-surface-separation` gives wheel compatibility and native distribution distinct owners. Python 3.12, 3.13, and 3.14 now each pass 573 tests in about one minute without building a native binary; the release session alone builds the native executable and passes 20 CLI and real-handoff black-box tests before packaging assets.
- The separated quality gate passes 573 tests with 97.02 percent combined coverage and 1490 of 1564 branches covered, strictly above the 95 percent branch floor. Explicit unit contracts now own native termination identity, signal failure, timeout, and confirmed-exit branches instead of relying on subprocess side effects for coverage.
- Signed commit `2e5404e4d782fffca4d85dceb640735c60dcc4ae` passed a fresh exact-head ETHOS proof with `python-matrix` and `python-quality` proven and no required gaps. The proof establishes the separated verification source and gates only; candidate landing, accepted closeout, hosted publication, installation, runtime acceptance, and retirement remain unproved post-archive transitions owned by this active claim.
- The active verification carrier also repairs the earlier malformed task boundary: candidate landing cannot be an unchecked pre-archive task because ETHOS requires archive before landing. The annotated-tag carrier and this carrier now retain only completed pre-archive work and state the still-pending lifecycle transitions without claiming them complete.
