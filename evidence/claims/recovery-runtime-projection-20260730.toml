[claim]
id = "recovery-runtime-projection-20260730"
subject = "codex-dmx-proxy:runtime-upgrade"
state = "active"
summary = "Local evidence binds the two-projection recovery repair."

[evidence]
dated = "evidence/chronicle/recovery-runtime-projection-20260730/2026-07-30.md"
sha256 = "ed06f40d221b854b61552dc6405701ac1ef0fc5e0f56bc1be9a2f8c33949b73f"
evidence_ids = ["evidence:recovery-runtime-projection-20260730"]
binding = "retained 1.0.35 runtime and committed 1.0.36 disk projection plus locally verified 1.0.41 repair"
verifier = "digest_only"
tests = [
  "python3 -m unittest tests.test_payload_transactions",
  "PYTHON=python3.12 RUFF=ruff TY=ty sh scripts/run-python-quality.sh",
  "for py in python3.12 python3.13 python3.14; do $py scripts/run-python-tests.py --compile; done",
  "openspec validate --all --strict --json",
]

[evidence.freshness]
mode = "historical"

[boundary]
owner = "codex-dmx-proxy"
scope = "Local recovery-contract evidence only; hosted CI, publication, installation, and runtime acceptance remain external."

[carriers]
openspec = "openspec/changes/recovery-runtime-projection"
fallback = "Keep the old listener serving and preserve the recovery transaction."
kill_signal = "A valid old runtime plus committed candidate projection remains impossible to recover."

[promotion]
targets = [
  "CHANGELOG.md",
  "VERSION",
  "codex_dmx_proxy/release/transaction.py",
  "openspec/specs/runtime-upgrade/spec.md",
  "tests/test_payload_transactions.py",
]
