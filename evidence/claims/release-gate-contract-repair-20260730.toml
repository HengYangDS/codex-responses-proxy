[claim]
id = "release-gate-contract-repair-20260730"
subject = "codex-dmx-proxy:ci-diagnostics"
state = "active"
summary = "Dated local evidence records release-gate checks and GitLab dependency-bootstrap diagnostics."

[evidence]
dated = "evidence/chronicle/release-gate-contract-repair-20260730/2026-07-30.md"
sha256 = "b088174ad50b43ae982f2928105f759af5bceea7556a1d011b47bb115322652b"
evidence_ids = ["evidence:release-gate-contract-repair-20260730"]
binding = "dated local release-gate and diagnostic-cleanliness checks for v1.0.38"
verifier = "digest_only"
tests = [
  "PYTHON=python3.12 RUFF=ruff TY=ty sh scripts/run-python-quality.sh",
  "for py in python3.12 python3.13 python3.14; do \"$py\" scripts/run-python-tests.py --compile; done",
  "python3 scripts/test_release_metadata.py",
  "python3 scripts/check_markdown_presentation.py",
  "openspec validate release-gate-contract-repair --strict --json",
]

[evidence.freshness]
mode = "historical"

[boundary]
owner = "codex-dmx-proxy"
scope = "Local release-gate repair evidence only; hosted CI, publication, installation, and runtime acceptance remain external."

[carriers]
openspec = "openspec/changes/archive/2026-07-30-release-gate-contract-repair"
fallback = "Retain the serving v1.0.35 runtime and the exact recovery transaction until a later release completes both Forge planes."
kill_signal = "A release gate still depends on private implementation text or a hosted dependency bootstrap emits an unhandled warning."

[promotion]
targets = [
  ".gitlab-ci.yml",
  "CHANGELOG.md",
  "VERSION",
  "openspec/specs/ci-diagnostics/spec.md",
  "scripts/test_release_metadata.py",
  "tests/test_quality_contract.py",
]
