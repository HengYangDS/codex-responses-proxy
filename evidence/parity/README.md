# Parity evidence

This directory is the durable root for parity artifacts admitted by ETHOS.
Codex DMX Proxy does not currently declare generic adopter parity; absence of a
`generic-shadow.json` file therefore means `not_configured`, not passing parity.

Dual-Forge release equality is a separate provider-publication concern. Its
live verifier and signed release receipts must not be copied here as generic
adopter parity.
